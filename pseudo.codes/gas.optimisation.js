// Rollup-specific optimizations:
// - Use of EIP-712 for signature efficiency
// - Storage in transient memory instead of contract storage
// - Merkle tree updates instead of individual storage writes
